#include <bits/stdc++.h>
using namespace std;
int main(){
    string label,opcode,operand;
    ifstream input("temp");
    string line;
    while(getline(input,line))
        cout<<line<<endl;
    return 0;
}